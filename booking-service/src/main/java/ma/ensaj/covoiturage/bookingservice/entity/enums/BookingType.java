package ma.ensaj.covoiturage.bookingservice.entity.enums;

public enum BookingType {
    FROM_TRIP_OFFER,    // passager réserve un trajet publié par driver
    FROM_RIDE_REQUEST   // driver répond à une demande de passager
}